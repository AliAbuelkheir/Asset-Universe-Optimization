# Defense Assets

Keep editable diagram sources (`.tex`, `.svg`, and `.py`) and one useful PNG
render for each diagram. Generated `.aux`, `.log`, standalone PDF, duplicate
opaque PNG, slide screenshot, and preview files are disposable.

`robin-logo.png` and `The_German_University_in_Cairo_Official_logo.jpg` are the
canonical logos.
