\documentclass[tikz,border=12pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,positioning}

\definecolor{navy}{HTML}{0C171F}
\definecolor{green}{HTML}{00F700}

\tikzset{
  card/.style={
    draw=navy,
    line width=1.2pt,
    rounded corners=5pt,
    minimum width=4.65cm,
    minimum height=2.45cm,
    text width=4.15cm,
    align=center,
    text=navy,
    fill=none,
    font=\fontsize{10.5}{13}\selectfont
  },
  flow/.style={
    -{Latex[length=3.4mm,width=2.2mm,fill=green]},
    draw=navy,
    line width=1.4pt
  },
  return/.style={
    flow,
    rounded corners=10pt
  },
  caption/.style={
    text=navy,
    font=\fontsize{10.5}{13}\selectfont
  }
}

\begin{document}
\begin{tikzpicture}
  \node[card] (state)
    {\textbf{\fontsize{14}{17}\selectfont Monthly Universe}\\[10pt]
     Active assets and features};
  \node[card, right=1.35cm of state] (action)
    {\textbf{\fontsize{14}{17}\selectfont Assign Scores}\\[10pt]
     One risk score per asset};
  \node[card, right=1.35cm of action] (reward)
    {\textbf{\fontsize{14}{17}\selectfont Evaluate Ranking}\\[10pt]
     Compare with realized risk};
  \node[card, right=1.35cm of reward] (update)
    {\textbf{\fontsize{14}{17}\selectfont Update Policy}\\[10pt]
     Improve future rankings};

  \draw[flow] (state.east) -- (action.west);
  \draw[flow] (action.east) -- (reward.west);
  \draw[flow] (reward.east) -- (update.west);
  \draw[return]
    (update.south) -- ++(0,-1.15cm) -| (state.south);

  \node[caption] at ($(state.south)!0.5!(update.south)+(0,-0.63cm)$)
    {Repeat for the next decision month};
\end{tikzpicture}
\end{document}
